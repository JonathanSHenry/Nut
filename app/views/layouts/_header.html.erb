<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "Nut", root_path, id: "logo" %>
        <%#= form_tag(search_path, :method => "get", class: 'navbar-form navbar-left') do %>  
        <#div class="input-group">  
          <#%= search_field_tag :search, params[:search], placeholder: "Search", class: "form-control" %>  
          <#div class="input-group-btn">  
            <#%= button_tag "", :class => 'btn btn-info glyphicon glyphicon-search',:name => nil%>  
          <#/div>  
       


  # Search if the name field contains...
  <%= search_form_for @q do |f| %>

  <%# Attributes may be chained. Search multiple attributes for one value...%>
  <%= f.label :name_or_description_or_menu_or_peanut_gluten_dairy_egg_soy_shellfish %>
  <%= f.search_field :name_or_description_or_menu_or_peanut_or_gluten_or_dairy_or_egg_or_soy_or_shellfish %>

    <%# Search if the name field contains...%>
  <%#= f.label :name_cont %>
  <%#= f.search_field :name_cont %>

  <%= f.submit %>
<% end %>

    </div>  

    <% end %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Home", root_path %></li>
        <li><%= link_to "Help", help_path %></li>
        <% if logged_in? %>
          <li><%= link_to "New Restaurant", restaurants_path %></li>
          <li><%= link_to "All Restaurants", all_restaurants_path %></li>
          <li><%= link_to "Users", users_path %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user %></li>
              <li><%= link_to "Settings", edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log out", logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Log in", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>